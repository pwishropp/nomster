  <div class="booyah-box col-10 offset-1">
  <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
  <ol class="carousel-indicators">
    <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
    <li data-target="#carouselExampleIndicators" data-slide-to="1"></li>
    <li data-target="#carouselExampleIndicators" data-slide-to="2"></li>
  </ol>
  <div class="carousel-inner" role="listbox">
    <div class="carousel-item active">
    <%= image_tag 'slider1.jpg', class: 'img-fluid' %>
     <div class="carousel-caption d-none d-md-block">
      <h3>Find the best dim sum places!</h3>
    </div>
    </div>
    <div class="carousel-item">
    <%= image_tag 'slider2.jpg', class: 'img-fluid' %>
    <div class="carousel-caption d-none d-md-block">
      <h3>Find the best sushi places!</h3>
    </div>
    </div>
    <div class="carousel-item">
    <%= image_tag 'slider3.jpg', class: 'img-fluid' %>
    <div class="carousel-caption d-none d-md-block">
      <h3>Find the best thai places!</h3>
    </div>
    </div>
  </div>
  <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>
<br />
<br />



    <% @places.each do |place| %>
    <h1><%= link_to place.name, place_path(place) %></h1>
    <small><i><%= place.address %></i></small>
    <br /><br />
    <p><%= place.description %></p>
    <% if place.user.present? %>
    <small><%= place.user.email %></small>
    <% end %>

<% if place.comments.present? %>
<div class="col-8">
  <blockquote>
    <% if place.comments.last.humanized_rating.present? %>
      <b><%= place.comments.last.humanized_rating %></b> -
    <% end %>
    <%= place.comments.last.message %>
    <br />
    <small><%= place.comments.last.user.email %></small>
  </blockquote>
</div>
<% end %>

<% if place.photos.present? %>
<div class="col-8">
  <blockquote>
    <% if place.photos.last.caption.present? %>
    <%= image_tag place.photos.last.picture.url, size: '250x250', class: 'image-fluid' %>
    <br />
    <br />
    <% end %>
    <b><%= place.photos.last.caption %></b> 
    <br />
    <small><%= place.photos.last.user.email %></small>
  </blockquote>
</div>
<% end %>

    
<% end %>
</div>


<%= paginate @places %>
